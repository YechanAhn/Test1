{
  "name": "@alrgo/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@alrgo/core-types": "workspace:*"
  }
}
